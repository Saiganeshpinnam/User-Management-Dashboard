User Management Dashboard — Full Stack Project

Backend:
  cd backend
  cp .env.example .env   # set MONGO_URI
  npm install
  npm run dev

Frontend:
  cd frontend
  npm install
  # Use any bundler or vite to run; source code in src/
